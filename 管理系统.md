# WhisperLiveKit 管理系统解决方案

## 1. 系统架构设计

### 1.1 整体架构
- **前端**：基于HTML5、CSS3和JavaScript，使用原生Web技术实现响应式界面
- **后端**：基于FastAPI扩展现有basic_server.py，添加管理系统API端点
- **数据存储**：
  - 用户认证数据：JSON文件
  - 配置参数：JSON文件
  - 专业术语：keywords.txt文件
  - 唤醒词：keywords_raw.txt和keywords.txt文件

### 1.2 技术选型
- **后端**：FastAPI（现有）、JWT认证、Pydantic数据验证
- **前端**：原生HTML/CSS/JavaScript、Fetch API、LocalStorage
- **安全**：JWT令牌、密码哈希、CORS配置
- **文件操作**：安全的文件读写操作，包含错误处理和数据验证

## 2. 模块划分

### 2.1 登录设置模块
- **功能**：用户身份验证
- **实现**：
  - JWT认证系统
  - 基本用户管理（登录、退出）
  - 注：暂时不需要权限控制（管理员/普通用户）和会话管理（token过期处理）

### 2.2 专业术语设置模块
- **功能**：管理专业术语，实现对keywords.txt文件的安全读写操作
- **实现**：
  - 基于现有KeywordsManager类扩展
  - 支持术语的添加、修改、删除
  - 文件格式验证和错误处理
  - 注：keywords.txt文件只有一个，不需要批量导入/导出功能

### 2.3 参数设置模块
- **功能**：提供影响语音识别准确率的关键参数配置
- **实现**：
  - 模型选择（Whisper模型类型）
  - 识别灵敏度（置信度阈值）
  - 语言设置（支持的语言列表）
  - 其他高级参数（如VAD设置、解码参数等）
  - 参数验证和默认值管理

### 2.4 唤醒词设置模块
- **功能**：管理唤醒词，实现对keywords_raw.txt和keywords.txt文件的安全管理
- **实现**：
  - 集成现有wake_keywords_converter.py工具
  - 支持唤醒词的新增、修改和删除
  - 自动转换为sherpa-onnx KWS模型所需格式
  - 支持boost值和threshold设置

## 3. 接口定义

### 3.1 认证接口
- `POST /api/auth/login` - 用户登录
- `POST /api/auth/logout` - 用户退出
- `GET /api/auth/me` - 获取当前用户信息

### 3.2 专业术语接口
- `GET /api/keywords` - 获取所有专业术语
- `POST /api/keywords` - 添加专业术语
- `PUT /api/keywords/{id}` - 修改专业术语
- `DELETE /api/keywords/{id}` - 删除专业术语

### 3.3 参数设置接口
- `GET /api/settings` - 获取当前参数设置
- `PUT /api/settings` - 更新参数设置
- `GET /api/settings/defaults` - 获取默认参数设置
- `POST /api/settings/reset` - 重置参数设置

### 3.4 唤醒词接口
- `GET /api/wakewords` - 获取所有唤醒词
- `POST /api/wakewords` - 添加唤醒词
- `PUT /api/wakewords/{id}` - 修改唤醒词
- `DELETE /api/wakewords/{id}` - 删除唤醒词
- `POST /api/wakewords/convert` - 转换唤醒词格式

## 4. 实现步骤

### 4.1 后端实现
1. **扩展basic_server.py**：添加管理系统API路由
2. **创建认证模块**：实现基本JWT认证和用户管理
3. **创建专业术语模块**：扩展KeywordsManager类，添加CRUD操作
4. **创建参数设置模块**：实现参数的存储和管理
5. **创建唤醒词模块**：集成wake_keywords_converter.py，实现唤醒词管理

### 4.2 前端实现
1. **创建管理系统界面**：登录页面、主控制台、各功能模块页面
2. **实现认证功能**：登录、退出
3. **实现专业术语管理界面**：术语列表、添加/编辑/删除功能
4. **实现参数设置界面**：参数配置表单、实时预览
5. **实现唤醒词管理界面**：唤醒词列表、添加/编辑/删除功能、格式转换

### 4.3 集成与测试
1. **集成前后端**：确保API接口正确对接
2. **安全测试**：验证认证功能
3. **功能测试**：测试各模块功能是否正常
4. **性能测试**：验证系统响应速度和稳定性

## 5. 文件结构

### 5.1 后端文件
- `WhisperLiveKit/basic_server.py` - 主服务器入口，添加管理系统API路由
- `WhisperLiveKit/auth.py` - 认证模块，实现基本JWT认证和用户管理
- `WhisperLiveKit/keywords_manager.py` - 专业术语管理模块（扩展现有文件）
- `WhisperLiveKit/settings_manager.py` - 参数设置管理模块
- `WhisperLiveKit/wakewords_manager.py` - 唤醒词管理模块
- `WhisperLiveKit/data/users.json` - 用户数据存储
- `WhisperLiveKit/data/settings.json` - 参数设置存储

### 5.2 前端文件
- `WhisperLiveKit/web/admin/` - 管理系统前端目录
  - `index.html` - 登录页面
  - `dashboard.html` - 主控制台
  - `keywords.html` - 专业术语管理页面
  - `settings.html` - 参数设置页面
  - `wakewords.html` - 唤醒词管理页面
  - `js/` - JavaScript文件
  - `css/` - 样式文件

## 6. 测试方案

### 6.1 功能测试
- **登录功能**：测试用户登录、退出
- **专业术语管理**：测试术语的添加、修改、删除
- **参数设置**：测试参数的修改、重置、默认值恢复
- **唤醒词管理**：测试唤醒词的添加、修改、删除、格式转换

### 6.2 安全测试
- **认证测试**：测试无效凭证
- **文件操作测试**：测试文件读写权限、错误处理
- **输入验证**：测试恶意输入、SQL注入防护

### 6.3 性能测试
- **响应时间**：测试API接口响应时间
- **并发测试**：测试多用户同时访问
- **稳定性测试**：测试长时间运行的稳定性

### 6.4 用户验收测试
- **功能验证**：验证所有功能是否符合需求
- **用户体验**：评估界面易用性、响应速度
- **兼容性**：测试不同浏览器的兼容性

## 7. 验收标准

1. **功能完整性**：所有核心功能模块正常工作
2. **安全性**：认证功能有效，无安全漏洞
3. **稳定性**：系统运行稳定，无崩溃或异常
4. **性能**：响应速度快，满足用户体验要求
5. **兼容性**：支持主流浏览器和设备
6. **代码质量**：代码结构清晰，注释完整，符合规范

## 8. 技术注意事项

1. **环境兼容性**：确保在项目虚拟环境中运行，所有依赖包与现有项目环境兼容
2. **文件操作安全**：实现错误处理和数据验证，确保文件操作的安全性和稳定性
3. **API接口设计**：遵循RESTful设计原则，提供清晰的接口文档
4. **前端交互**：实现响应式设计，确保在不同设备上的良好体验
5. **错误处理**：实现全面的错误处理机制，提供友好的错误提示
6. **日志记录**：实现详细的日志记录，便于问题排查和系统监控

此解决方案提供了一个完整的管理系统框架，可根据实际需求进行调整和扩展。